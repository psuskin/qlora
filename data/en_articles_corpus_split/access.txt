access: Basic modules for partner functions. Functionality: See partner functions (e.g.: customers, suppliers). Module name: access.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: accessc.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: bank.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: bankedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: banksel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: customer.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: custoedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: custosel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: cyber.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: cyberedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: cybersel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: flextime.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: flextedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: flextsel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: forwardr.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: forwaedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: forwasel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: manufact.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: manufedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: manufsel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: potpartn.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: potpaedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: potpasel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: press.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: pressedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: press.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: representations.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: repreedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: represel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: supplier.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: suppledt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: supplsel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: accessp.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: partner.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: partnerc.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: partnedc.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: partnsec.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: partnerp.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: partnedp.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: partnsep.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: partnsel.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: taxauth.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: taxaedt.mod. Classes: CX_ACCESS. Security: In addition to restricting access rights via the class and its data fields, the module can be restricted in its use via some of the received messages. Module name: taxasel.mod. Classes: CX_ACCESS.