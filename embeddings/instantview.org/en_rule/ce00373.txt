This is the description of the class "CX_VALUE - Arithmetic": The following overviews show the mathematical operators and the operands to which a real number can be linked (all results correspond to the priority rules): Addition Subtraction Multiplication Division Precision The precision of the calculation is usually determined by the first value. 1,0 + 1,0000 = 2,0 1,000 m + 1m = 1,000m An exception is when a CX_NUMERIC meets a CX_VALUE or a CX_VALUE_PER. In this case the latter objects are of higher value and overwrite the precision. 1,0 + 1,0000m = 2,0000m 1,000 m + 1 = 1,000m Addition CX_VALUE + INTEGER = CX_VALUE 2,4m + 10 = 12,4m(5) INTEGER + CX_VALUE = CX_VALUE 10 + 2,4m = 12,4m(5) CX_VALUE + CX_NUMERIC = CX_VALUE 2,4m + 10 = 12,4m(5) CX_NUMERIC + CX_VALUE = CX_VALUE 10 + 2,4m = 12,4m(5) CX_VALUE + CX_FRACTION = CX_VALUE 2,4m + 1/2 = 2,9m(5) CX_FRACTION + CX_VALUE = CX_VALUE 1/2 + 2,4m = 2,9m(5) Note: When adding whole or real numbers to a value, they "get" the same unit as the value for the operation. CX_VALUE + CX_VALUE = CX_VALUE 2,4m + 1,4m = 3,8m 2,4 m + 20 cm = 2,6m 20 cm + 2,4 m = 260cm Note: When adding up values, the unit of the result depends on the first operand. CX_VALUE + CX_PERCENT = CX_VALUE 2,4m + 50% = 3,6m(3.1) CX_PERCENT + CX_VALUE = Error 50% + 2,4m = not allowed(2.1) CX_VALUE + CX_VALUE_PER = CX_VALUE_PER 2 € + 1 €/2 m = Unit error 2 m + 1 €/2 m = Unit error 2 €/m + 1 €/2 m = 5€ / 2m CX_VALUE_PER + CX_VALUE = CX_VALUE_PER 1 €/2 m + 2 €/m = 5 €/2 m CX_VALUE + CX_FORMULA = CX_VALUE 2,4m + Formula = Number CX_FORMULA + CX_VALUE = CX_VALUE Formula + 2,4m = Number Note: Before the addition is performed, the formula is evaluated. The actual addition is only executed if the result is an object which may be added to a value. If this is the case, number is the result. CX_VALUE + CX_CONDITIONED_BAG = CX_VALUE 2,4m + conditional object table = Number CX_CONDITIONED_BAG + CX_VALUE = CX_VALUE conditional object table + 2,4m = Number Note: Before the addition is executed, the corresponding object is determined from the conditional object table. The actual addition is only executed if this object may be added to a value. If this is the case, the result is a number . Subtraction CX_VALUE - INTEGER = CX_VALUE 2,4m - 2 = 0,4 m(5) INTEGER - CX_VALUE = CX_VALUE 2 - 2,4m = -0,4 m(5) CX_VALUE - CX_NUMERIC = CX_VALUE 2,4 m - 1,4 = 1 m(5) CX_NUMERIC - CX_VALUE = CX_VALUE 1,4 - 2,4 m = -1 m(5) CX_VALUE - CX_FRACTION = CX_VALUE 2,4 m - 1/2 = 1,9 m(5) CX_FRACTION - CX_VALUE = CX_VALUE 1/2 - 2,4 m = -1,9 m(5) Note: When subtracting whole or real numbers to a value, they "get" the same unit as the value for the operation. CX_VALUE - CX_VALUE = CX_VALUE 2,4 m - 1,4 m = 1 m 2,4 m - 30 cm = 2,1 m 30 cm - 2,4 m = -210 cm Note: When subtracting values, the unit of the result depends on the first operand. CX_VALUE - CX_PERCENT = CX_VALUE 2,4m - 50% = 1,2 m(3.1) CX_PERCENT - CX_VALUE = CX_VALUE 50% - 2,4m = not allowed(2.1) CX_VALUE - CX_VALUE_PER = CX_VALUE_PER 2 € - 1 €/2 m = Unit error 2 m - 1 €/2 m = Unit error 2 €/m - 1 €/2 m = 3 €/2 m CX_VALUE_PER - CX_VALUE = CX_VALUE_PER 1 €/2 m - 2 €/m = -3 €/2 m CX_VALUE - CX_FORMULA = CX_VALUE 2,4m - Formula = Number CX_FORMULA - CX_VALUE = CX_VALUE Formula - 2,4m = Number Note: Before the subtraction is performed, the formula is evaluated. The actual subtraction is only executed if the result is an object which may be subtracted from a value. If this is the case, number is the result. CX_VALUE - CX_CONDITIONED_BAG = CX_VALUE 50% - conditional object table = Number CX_CONDITIONED_BAG - CX_VALUE = CX_VALUE conditional object table - 50% = Number Note: Before the subtraction is executed, the corresponding object is determined from the conditional object table. The actual subtraction is only executed if this object may be subtracted from a value. If this is the case, the result is a number . Multiplication CX_VALUE * INTEGER = CX_VALUE 2,4 m * 2 = 4,8 m INTEGER * CX_VALUE = CX_VALUE 2 * 2,4 m = 4,8 m CX_VALUE * CX_VALUE = CX_VALUE 2,4 m * 50 cm = 120 dm2 2,4 m * 2 1/m = 4,8 4 kg*m/s * 3 1/s = 12 N Note: When two values are multiplied, the result is given a new unit. In special cases the result may be unitless . CX_VALUE * CX_NUMERIC = CX_VALUE 2,4 m * 2 = 4,8 m CX_NUMERIC * CX_VALUE = CX_VALUE 2 * 2,4 m = 4,8 m CX_VALUE * CX_FRACTION = CX_VALUE 2,4 m * 1/2 = 1,2 m CX_FRACTION * CX_VALUE = CX_VALUE 1/2 * 2,4 m = 1,2 m CX_VALUE * CX_PERCENT = CX_VALUE 2,4 m * 50% = 1,2 m(3) CX_PERCENT * CX_VALUE = Error 50% * 2,4 m = not allowed(2.1) CX_VALUE * CX_VALUE_PER = CX_VALUE 2,4 m * 1 €/2 piece = 1,2 €-m/piece(1.2) CX_VALUE_PER * CX_VALUE = CX_VALUE 1 €/2 piece * 2,4 m = 1,2 €-m/piece(1.2) CX_VALUE * CX_FORMULA = CX_VALUE 2,4 m * Formula = Number CX_FORMULA * CX_VALUE = CX_VALUE Formula * 2,4 m = Number Note: Before the multiplication is performed, the formula is evaluated. The actual multiplication is only executed if the result is an object which may be multiplied by a value. If this is the case, number is the result. CX_VALUE * CX_CONDITIONED_BAG = CX_VALUE 2,4 m * conditional object table = Number CX_CONDITIONED_BAG * CX_VALUE = CX_VALUE conditional object table * 2,4 m = Number Note: Before the multiplication is executed, the corresponding object is determined from the conditional object table. The actual multiplication is only executed if this object may be multiplied to a value. If this is the case, the result is a number . Division CX_VALUE / INTEGER = CX_VALUE 2,4 m / 2 = 1,2 m INTEGER / CX_VALUE = CX_VALUE 2 / 2,5 m = 0,8 1/m CX_VALUE / CX_VALUE = CX_VALUE 2,4 m / 3 s = 0.8 m/s 2,4 m / 3 cm = 80 4 m*kg/s / 2 s = 2 N Note: When dividing two values, the result is given a new unit. In special cases the result may be unitless . CX_VALUE / CX_NUMERIC = CX_VALUE 2,4 m / 2 = 1,2 m CX_NUMERIC / CX_VALUE = CX_VALUE 2 / 2,5 m = 0,8 1/m CX_VALUE / CX_FRACTION = CX_VALUE 2,4 m / 1/2 = 4,8 m CX_FRACTION / CX_VALUE = CX_VALUE 1/2 / 2,4 m = 0,20833 1/m CX_VALUE / CX_PERCENT = CX_VALUE 2,4 m / 50% = 4,8 m(3) CX_PERCENT / CX_VALUE = Error 50% / 2,4 m = not allowed(2.1) CX_VALUE / CX_VALUE_PER = CX_VALUE 2 m / 1 €/2 piece = 4 m piece/€(1.2) CX_VALUE_PER / CX_VALUE = CX_VALUE 1 €/2 piece / 2 m = 0,25 €/m piece(1.2) CX_VALUE / CX_FORMULA = CX_VALUE 2,4 m / Formula = Number CX_FORMULA / CX_VALUE = CX_VALUE Formula / 2,4 m = Number Note: Before the division is executed, the formula is evaluated. The actual dividing is only executed if the result is an object which is allowed to divide a value. If this is the case, number is the result. CX_VALUE / CX_CONDITIONED_BAG = CX_VALUE 2,4 m / conditional object table = Number CX_CONDITIONED_BAG / CX_VALUE = CX_VALUE conditional object table / 2,4 m = Number Note: Before the division is executed, the corresponding object is determined from the conditional object table. The actual division is only executed if this object is allowed to divide a value. If this is the case, the result is a number .