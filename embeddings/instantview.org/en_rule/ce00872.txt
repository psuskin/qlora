This is the description of the class "Static (object unbound) function": A function which is called via SYSTEM Call() does not belong to an object of a certain class. It represents a generally valid calculation routine (the function Barcode25Interleaved() calculates the representation for the barcode 2/5 Interleaved() from any character string or it provides information from the system (e.g. the function GetLocale()).