Dies ist die Beschreibung der Klasse "CX_PERCENT - Vergleiche": Die folgenden Übersichten zeigen die Vergleichsoperatoren und die Operanden mit denen eine Prozentzahl verknüpft werden kann. Alle Operatoren geben das Ergebnis des Vergleichs als Wahrheitswert zurück. Gleichheit Größer Kleiner Gleichheit CX_PERCENT = INTEGER = INTEGER <-> 2% = 1 = 0 2% = 2 = 1 2% = 3 = 0 CX_PERCENT = CX_NUMERIC = INTEGER <-> 2,5% = 1,5 = 0 2,5% = 2,5 = 1 2,5% = 3,5 = 0 CX_PERCENT = CX_PERCENT = INTEGER <-> 2% = 1% = 0 2% = 2% = 1 2% = 3% = 0 CX_PERCENT = CX_VALUE = INTEGER -> 2% = 1 Stück = 0 2% = 2 Stück = 1 2% = 3 Stück = 0 CX_PERCENT = CX_FRACTION = INTEGER -> 2,5% = 3/4 = 0 2,5% = 5/2 = 1 2,5% = 7/2 = 0 CX_PERCENT = CX_FORMULA = INTEGER -> 10% = Formel = Zahl Anmerkung: Bevor der Vergleich ausgeführt wird, wird die Formel ausgewertet. Das eigentliche Vergleichen wird nur dann ausgeführt, wenn das Ergebnis ein Objekt ist, welches mit einer Prozentzahl verglichen werden darf. Ist dieses der Fall, so entsteht Zahl als Ergebnis. CX_PERCENT = CX_CONDITIONED_BAG = INTEGER -> 10% = bedingte Objekttabelle = Zahl Anmerkung: Bevor der Vergleich ausgeführt wird, wird das entsprechende Objekt aus der bedingten Objekttabelle ermittelt. Das eigentliche Vergleichen wird nur dann ausgeführt, wenn dieses Objekt mit einer Prozentzahl verglichen werden darf. Ist dieses der Fall, so entsteht Zahl als Ergebnis. Größer CX_PERCENT > INTEGER = INTEGER <-> 2% > 1 = 1 2% > 2 = 0 2% > 3 = 0 CX_PERCENT > CX_NUMERIC = INTEGER <-> 2,5% > 1,5 = 1 2,5% > 2,5 = 0 2,5% > 3,5 = 0 CX_PERCENT > CX_PERCENT = INTEGER <-> 2% > 1% = 1 2% > 2% = 0 2% > 3% = 0 CX_PERCENT > CX_VALUE = INTEGER -> 2% > 1 Stück = 1 2% > 2 Stück = 0 2% > 3 Stück = 0 CX_PERCENT > CX_FRACTION = INTEGER -> 2,5% > 3/4 = 1 2,5% > 5/2 = 0 2,5% > 7/2 = 0 CX_PERCENT > CX_FORMULA = INTEGER -> 10% > Formel = Zahl Anmerkung: Bevor der Vergleich ausgeführt wird, wird die Formel ausgewertet. Das eigentliche Vergleichen wird nur dann ausgeführt, wenn das Ergebnis ein Objekt ist, welches mit einer Prozentzahl verglichen werden darf. Ist dieses der Fall, so entsteht Zahl als Ergebnis. CX_PERCENT > CX_CONDITIONED_BAG = INTEGER -> 10% > bedingte Objekttabelle = Zahl Anmerkung: Bevor der Vergleich ausgeführt wird, wird das entsprechende Objekt aus der bedingten Objekttabelle ermittelt. Das eigentliche Vergleichen wird nur dann ausgeführt, wenn dieses Objekt mit einer Prozentzahl verglichen werden darf. Ist dieses der Fall, so entsteht Zahl als Ergebnis. Kleiner CX_PERCENT < INTEGER = INTEGER <-> 2% < 1 = 0 2% < 2 = 0 2% < 3 = 1 CX_PERCENT < CX_NUMERIC = INTEGER <-> 2,5% < 1,5 = 0 2,5% < 2,5 = 0 2,5% < 3,5 = 1 CX_PERCENT < CX_PERCENT = INTEGER <-> 2% < 1% = 0 2% < 2% = 0 2% < 3% = 1 CX_PERCENT < CX_VALUE = INTEGER -> 2% < 1 Stück = 0 2% < 2 Stück = 0 2% < 3 Stück = 1 CX_PERCENT < CX_FRACTION = INTEGER -> 2,5% < 3/4 = 0 2,5% < 5/2 = 0 2,5% < 7/2 = 1 CX_PERCENT < CX_FORMULA = INTEGER -> 10% < Formel = Zahl Anmerkung: Bevor der Vergleich ausgeführt wird, wird die Formel ausgewertet. Das eigentliche Vergleichen wird nur dann ausgeführt, wenn das Ergebnis ein Objekt ist, welches mit einer Prozentzahl verglichen werden darf. Ist dieses der Fall, so entsteht Zahl als Ergebnis. CX_PERCENT = CX_CONDITIONED_BAG = INTEGER -> 10% = bedingte Objekttabelle = Zahl Anmerkung: Bevor der Vergleich ausgeführt wird, wird das entsprechende Objekt aus der bedingten Objekttabelle ermittelt. Das eigentliche Vergleichen wird nur dann ausgeführt, wenn dieses Objekt mit einer Prozentzahl verglichen werden darf. Ist dieses der Fall, so entsteht Zahl als Ergebnis.