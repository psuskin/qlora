Dies ist die Beschreibung der Funktionalität der Klasse "CX_BOOLEAN" bezüglich Beschreibung: Diese Klasse bildet der Wahrheitswert in System ab. Unter einem Wahrheitswert wird ein Wert verstanden, der entweder "Ja" (= TRUE) oder "Nein" (= FALSE) sein kann. Somit kann er z.B. anzeigen, ob eine bestimmte Bedingung erfüllt ist oder nicht. Für diese beiden Zustände werden folgende Werte verwendet:. Dies ist die Beschreibung der Funktionalität der Klasse "CX_BOOLEAN" bezüglich Code-Beispiel: Diese Klasse besitzt weder Funktionen noch Datenfelder.