Dies ist die Beschreibung des Moduls "offorita" mit dem Namen "Angebots-Auftragsattribute Bearbeitungsfenster": Sind in einem Auftrag oder Angebot viele gleiche Variantenteile mit immer leicht unterschiedlichen Ausprägungen vorhanden, ist es wirtschaftlicher, für alle die selben Ausprägungen verwenden zu können. Um die Attribute schnell und übersichtlich gleich einstellen zu können, ist dieses Fenster da. Dies ist die Beschreibung der Funktionalität des Moduls "offorita" mit dem Namen "Angebots-Auftragsattribute Bearbeitungsfenster" bezüglich Eingabeliste: Diese Liste repräsentiert alle übergebenen Auftragspositionen mit deren festgelegten Attributen. übersichtlich untereinander. Wird ein Eintrag doppelt angeklickt, so werden seine Attribute im Positions-Bearbeitungsfenster angezeigt, wo der Wert manuell angepasst werden kann. Wurden in der Liste mehrere Auftragspositionen markiert und die Bearbeitungsmaske über den Bearbeiten Knopf geöffnet, so werden alle Änderungen der Attribute für alle selektierten Positionen durchgeführt. Ebenfalls funktioniert so die Umsetzung von bedingten in voreingestellte Attribute und umgekehrt. Dies ist die Beschreibung der Funktionalität des Moduls "offorita" mit dem Namen "Angebots-Auftragsattribute Bearbeitungsfenster" bezüglich Bearbeitungsfenster: In diesem Fenster werden alle Attribute der ersten markierten Auftragsposition angezeigt. Durch einen Doppelklick auf die entsprechende Listenzeile (Attribut) wird der manuelle Wert für Änderungen freigegeben. Nun können hier verschiedene Werte angegeben und beobachtet werden, wie sich für die weiteren Attribute diese Änderungen auswirken. Sind weitere Attribute bedingte oder Formelattribute und abhängig vom soeben veränderten, so werden die Werte nach einem Klick auf den Knopf "Kalkulieren" aktualisiert. Damit die Übersichtlichkeit über die Änderungen erhalten bleibt, teilt sich die Liste in 3 Spalten auf: Sind in der Liste bedingte oder errechnete Attribute vorhanden, dessen Ergebnis aber noch leicht angepasst werden soll, gibt es über das Kontextmenü (mit der rechten Maustaste auf den Listeneintrag klicken) die Möglichkeit, diese bedingten und Formelattribute in voreingestellte umzuwandeln (Formel / Bedingung durch festen Wert ersetzen). Dabei erhält das neue voreingestellte Attribute den Wert, den auch das errechnete vorher hatte. Nun kann dies auch manuell verändert werden. Bitte beachten Sie aber, dass nach einer solchen Umsetzung dieses bedingte Attribut auf keinerlei Veränderungen von anderen Attributen mehr reagiert! Es hat nun seinen festen Wert! Ebenfalls ist zu beachten, dass in den Stammattributen für jedes der bedingten und Formelattribute ein voreingestelltes Attribut mit dem selben Datenfeld vorhanden ist! Soll das neue voreingestellte Attribut doch wieder in seine bedingte Ursprungsform zurück gewandelt werden, so kann dies ebenfalls über das Kontextmenü geschehen. Hier gibt es noch einen weiteren Eintrag, der diese Funktion übernimmt (Formel / Bedingung wiederherstellen). Die Änderungen werden erst dann auf die Auftragspositionen übernommen, wenn auf dem Listenfenster der Knopf "OK" betätigt wird!